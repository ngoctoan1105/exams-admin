export interface ICategoryData {
  isAdd: boolean;
  name: string;
  description: string;
}
export interface ICategory {
  id: string;
  name: string;
  description: string;
}

export interface ICategoryReq {
  name: string;
  description: string;
}
